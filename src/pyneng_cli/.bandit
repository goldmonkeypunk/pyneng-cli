[bandit]
# Перевіряємо лише наш продакшен‑код
targets = src

# Не ліземо у віртуалку та тести
exclude = .venv,tests

# «Червона» збірка тільки на HIGH‑рівні
severity = HIGH
confidence = HIGH

# Текстовий репорт, добре читається у GitHub Actions
output_format = txt
